#include <iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "ru");
	int N, K;

	// Ввод количества белок и орехов
	cout << "Введите количество белок: ";
	cin >> N;
	cout << "Введите количество орехов: ";
	cin >> K;
	// Определение остатка
	int ostatok = K % N;
	// Вывод результата
	cout << "Останется " << ostatok << " орехов." << endl;
	int main() {
		int h1, m1, s1, h2, m2, s2;

		// Ввод времени первого момента
		cout << "Введите время первого момента (часы минуты секунды): ";
		cin >> h1 >> m1 >> s1;
		// Ввод времени второго момента
		cout << "Введите время второго момента (часы минуты секунды): ";
		cin >> h2 >> m2 >> s2;
		// Проверка на корректность ввода
		if (h1 < 0 || h1 > 23 || m1 < 0 || m1 > 59 || s1 < 0 || s1 > 59 ||
			h2 < 0 || h2 > 23 || m2 < 0 || m2 > 59 || s2 < 0 || s2 > 59 ||
			(h2 == h1 && m2 == m1 && s2 < s1)) {
			cout << "Некорректный ввод времени." << endl;
			return 1;
		}
		// Вычисление разницы в секундах
		int total_seconds1 = h1 * 3600 + m1 * 60 + s1;
		int total_seconds2 = h2 * 3600 + m2 * 60 + s2;
		int difference = total_seconds2 - total_seconds1;

		// Вывод результата
		cout << "Между двумя моментами времени прошло " << difference << " секунд." << endl;
		return 0;
	}